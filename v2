#include<stdio.h>
float power(float x,int n)
{ int i;
  float s;
  s=1;
  for (i=0;i<n;i++) s=s*x;
  return s;
}
 
int main()
{ float y; float k; int rrr;
  k=2.125; rrr=10.0;
  y=power(k, rrr);
  printf("Result: %f\n",y);
  return 0;
}
